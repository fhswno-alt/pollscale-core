{
  "name": "@pollscale/api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "uv run pytest",
    "seed": "uv run python -m scripts.seed"
  }
}
